import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sidemenu_test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
